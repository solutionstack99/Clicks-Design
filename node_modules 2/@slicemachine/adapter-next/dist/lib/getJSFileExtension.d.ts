import { SliceMachineContext } from "@slicemachine/plugin-kit";
import { PluginOptions } from "../types";
type GetJSFileExtensionArgs = Pick<SliceMachineContext<PluginOptions>, "helpers" | "options"> & {
    jsx?: boolean;
};
export declare const getJSFileExtension: ({ helpers, options, jsx, }: GetJSFileExtensionArgs) => Promise<string>;
export {};
